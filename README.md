# docker-laravel-handson

## ディレクトリ構成
```
├── docker
│   └── php
│       ├── Dockerfile
│       └── php.ini # PHPの設定ファイル
├── docker-compose.yml
├── logs
│   └── php-error.log
└── src # Laravelをインストールするディレクトリ
```